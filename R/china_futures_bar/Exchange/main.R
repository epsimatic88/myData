################################################################################
setwd('/home/fl/myData/')
################################################################################

################################################################################
## 建立数据文件
## ./data/
################################################################################
exchID <- c('CFFEX','CZCE','DCE','SHFE')

for (i in exchID) {
  temp <- paste0("./data/Bar/Exchange/",i)

  if (!dir.exists(temp)) dir.create(temp)
}
################################################################################

